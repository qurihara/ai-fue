__pycache__/
*.pyc
.DS_Store

# 全出力は out/ にフラットに集約し、丸ごと git 管理する（2026/7/14〜）。
# STL・印刷3mf/swap・版スナップショットも全部 out/ 直下に置いて追跡。
# 例外：PNG レンダー（診断画像）は再生成物なので無視。
out/*.png

# temp/ のスライス中間物・gcode・展開物は無視（再生成可）。
# 印刷プロファイル temp/*_profile.json はレシピなので追跡する。
temp/*
!temp/*_profile.json
result.json
